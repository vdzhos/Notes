<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:paddingStart="@dimen/drawer_app_name_side_padding"
        android:paddingEnd="@dimen/drawer_app_name_side_padding"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/name"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/drawer_app_name_text_view_height"
            android:fontFamily="@font/roboto_medium"
            android:paddingBottom="2dp"
            android:gravity="bottom"
            android:textSize="22sp" />

        <TextView
            android:id="@+id/keep"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/drawer_app_name_text_view_height"
            android:fontFamily="@font/roboto"
            android:paddingBottom="2dp"
            android:gravity="bottom"
            android:text="@string/app_name_keep"
            android:textSize="20sp" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>